<style>
    .header {
        background-color: #404356 !important;
    }
</style>

<main class="main">
    <style>
        .accordion-button:not(.collapsed) {
            color: var(--bs-accordion-active-color);
            background-color: transparent !important;
            box-shadow: inset 0 calc(-1* var(--bs-accordion-border-width)) 0 var(--bs-accordion-border-color);
        }
    </style>
    <br><br><br><br>
    <div class="container">


        <section class="section">
            <div class="row">
                <div class="col-lg-12">
                    <h3><B>ALL SURVEYS</B></h3> <br>

                    <div class="card p-3 shadow">
                        <div class="table-responsive">
                            <table class="table border table-bordered">
                               <thead>
                            <tr>
                                 <th scope="col">No.</th>
                                 <th data-type="date" data-format="YYYY/DD/MM">Added Date</th>
                                <th scope="col">Title</th>
                                <th scope="col">Class</th>
                                <th scope="col">Subject</th>
                                <th scope="col">Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            {{#each result}}
                            <tr>
                                <th scope="row">{{incremented @index}}</th>
                                <th>{{formatDate this.createdAt}}</th>
                                <td>{{this.assessmentTitle}}</td>
                                <td>{{this.classname}}</td>
                                <td>{{this.subject}}</td>
                               <td> {{#isUserInArray this.doneUser ../user._id}}
    <button class="btn btn-success" disabled>Already Submitted</button>
  {{else}}
    <a href="/take-assesment/{{this._id}}" class="btn btn-success">Take Survey</a>
  {{/isUserInArray}}
                             </td>
                            </tr>
                            {{/each}}
                        </tbody>
                            </table>
                        </div>
                    </div>








                </div>
            </div>
        </section>
    </div>
</main>